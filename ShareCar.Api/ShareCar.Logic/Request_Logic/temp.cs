using ShareCar.Db.Entities;
using ShareCar.Dto.Identity;
using ShareCar.Logic.ObjectMapping;
using System;
using System.Collections.Generic;
using System.Text;

namespace ShareCar.Logic.Request_Logic
{
    class temp
    {
        /*
      private readonly RequestRepository _requestRepository;
        private readonly RequestMapper _requestMapper;

        public IEnumerable<Request> GetAllRequests(string driverEmail)
        {
            List<Request> Requests = _requestRepository.GetRequestsByDriver(;

        }

        public IEnumerable<Request> GetSeenRequests(List<)

                    public IEnumerable<RequestDto> CheckDriverRequests(string driverEmail)
        {
            List<Request> Requests = (List<Request>)_requestRepository.GetRequestsByUser(driverEmail);
            List<RequestDto> DtoRequests = new List<RequestDto>();

            foreach (var r in Requests)
            {
                // Mapping
            }

            return DtoRequests;

        }

        public IEnumerable<RequestDto> CheckrRequests(string driverEmail)
        {
            List<Request> Requests = (List<Request>)_requestRepository.GetRequestsByUser(driverEmail);
            List<RequestDto> DtoRequests = new List<RequestDto>();

            foreach(var r in Requests)
            {
                // Mapping
            }

            return DtoRequests;

        }
        */
    }
}
